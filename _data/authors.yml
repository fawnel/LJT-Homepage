# Authors
#
# Multi-author configuration for the Minimal Mistakes theme. This is a
# single-author site, so the sidebar and SEO use the `author:` block in
# `_config.yml` instead. No additional authors are configured here.
